# Demo Script — Judge Cut v2 (1:39)

Language: English voiceover and English UI. The final file is 1920×1080 with audio and stays well below Devpost's three-minute limit.

Final video: `H:\Sync\cowork\risk-sovereignty-demo-video\risk-sovereignty-build-week-judge-demo-v2.mp4`

## 0:00–0:44 — Product thesis and architecture

Show the product name, three supported U.S. balance-sheet structures, asset-return lifeline, deterministic engine, structural evidence, GPT-5.6 tool workflow, and auditable boundary.

Core message: Risk Sovereignty does not forecast the future. It constructs a visible bad future, finds the first point that removes optionality, and helps the user preserve a staged exit.

## 0:44–0:52 — Start with a survivable structure

**Screen:** Manufacturing example with every bad-weather slider at zero.

**Visible result:** Signal, 36.0+ months, Concentration is the first weak point.

**Voice:**

> Now the basic user workflow. Start with a survivable baseline.

## 0:52–1:00 — Drag the stress controls

**Screen:** Increase revenue decline, margin compression, fixed cost, payment delay, customer loss, called debt, and inventory impairment.

**Voice:**

> Drag revenue, margin, cost, payment delay, customer loss, debt, and inventory controls.

## 1:00–1:08 — Watch the stage change

**Screen:** The same balance sheet reaches Trend.

**Visible result:** 9.3 months, Collection is now the first failure point.

**Voice:**

> The same balance sheet moves from Signal, to Trend, to Emergency. Runway collapses from more than thirty-six months, to nine point three, then zero. The first failing lifeline changes from concentration, to collection, to cash.

## 1:08–1:16 — Show the exit disappearing

**Screen:** Severe stress case.

**Visible result:** Emergency, 0.0 months, negative buffer, Cash fails first.

## 1:16–1:24 — Ask one simple English question

**Screen:** AI Red Team section with this exact prompt:

> Revenue is falling and our largest customer pays late. What should I unwind first to survive without trapping the business?

## 1:24–1:32 — Show the verified GPT-5.6 answer

**Screen:** Verified result and causal stress chain.

**Voice:**

> GPT-5.6 receives the deterministic engine output before it speaks. It does not invent the numbers. It identifies the binding constraint, proves whether a partial exit is real, and returns three staged actions.

## 1:32–1:39 — End on the method

**Screen:** Sovereignty gate, three staged actions, critical assumptions, API proof, and auditable boundary.

**Voice:**

> Stop the bleeding, preserve an exit, and rebuild optionality. We are not predicting the future. We are making sure a bad future cannot remove every next move.

## Verified live result used in the video

- Stage: Emergency
- Runway: 0 months
- Immediate liquidity shock: $496.3k against $310k cash
- First failure: Cash
- GPT-5.6 recommendation: freeze new customer-specific exposure first, not the whole business; seek a debt standstill and staged inventory release; then rebuild cash reserves, billing milestones, deposits, and exposure caps.
- The displayed audit strip includes `gpt-5.6`, forced function calling, deterministic engine, strict structured output, and both OpenAI response IDs.

## Recording rules

- Never show an API key, billing page, account email, bookmarks, or unrelated tabs.
- Use one real GPT-5.6 request only; do not claim a fallback as AI output.
- Keep the product and its calculation trace legible at 1080p.
- End on the product and the core line: “Do not predict the future. Control exposure. Preserve an exit.”
